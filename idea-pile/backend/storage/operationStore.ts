class OperationStore{
    /*
private db: DatabaseClient;

append(
  docId: string,
  version: number,
  delta: Delta
): Promise<void>;

getRange(
  docId: string,
  fromVersion: number,
  toVersion: number
): Promise<Delta[]>;*/
}

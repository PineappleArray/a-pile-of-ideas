export interface Snapshot {
  content: string;
  revision: number;
  createdAt: number;
}


class SnapshotStore {
    /*
saveSnapshot(docId: string, version: number, text: string): Promise<void>;
loadLatestSnapshot(docId: string): Promise<{ version: number; text: string } | null>;*/

}